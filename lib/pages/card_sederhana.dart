import 'package:flutter/material.dart';

class cardsimpel extends StatelessWidget {
  const cardsimpel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("card sederhana"),
          actions: const[],
        ),
      body: SingleChildScrollView(
        
      ),
    );
  }
}